# `git4ol` Records

`git4ol-records` records the events generated by [`git4ol-events`](https://github.com/open-learning/git4ol-events/) and provides services to query them as part of [`git4ol-infrastructure`](https://github.com/open-learning/git4ol-infrastructure/).
